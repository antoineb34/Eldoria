# Runtime

Systems and supporting types that operate on decoded assets while Eldoria is
running.

- `math/` shared runtime math primitives
- `render/` rendering resources, scene, pipeline, and backends
- `audio/` audio/MIDI playback
- `world/` live world representation
- `game/` gameplay/runtime rules
- `network/` client/server communication
