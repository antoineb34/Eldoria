# Runtime Render

The complete rendering subsystem.

This includes render resources, asset-to-render resolution, model/animation/map
preparation, scene construction, camera/projection, pipelines, and concrete
rendering backends.

Source/cache decoding belongs in `src/assets`, not here.
