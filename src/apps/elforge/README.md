# ElForge

ElForge is the internal Eldoria development tool.

This skeleton intentionally starts small:

- application entry point
- minimal application lifetime class
- startup/shutdown output
- standalone CMake target

Future issues will add cache inspection, asset viewing, debugging panels, editing workflows, and integration with shared modules.
