# ElClient

ElClient is the playable Eldoria game client.

This skeleton intentionally starts small:

- application entry point
- minimal application lifetime class
- startup/shutdown output
- placeholder client state
- placeholder screen enum
- standalone CMake target

Future issues will add windowing, rendering, input, networking, cache loading, login flow, world state, and UI integration.
