# data

Purpose: owns what Eldoria things are.

The data module is responsible for content definitions and asset/data loading concepts such as cache data, binary formats, models, textures, maps, animations, items, NPCs, objects, interfaces, and sprites.

Dependency rule: `data` should stay independent from runnable apps and should not depend on rendering, UI, or platform-specific application code.
