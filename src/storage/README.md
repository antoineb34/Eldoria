# Storage

Storage/container infrastructure used to retrieve encoded source data.

- `cache/` understands the physical cache/store/index/sector layout.
- `archive/` understands the packed archive container format.

Storage does not interpret files as game assets.
