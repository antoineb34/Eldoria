# Network target note

The module directory is named `net` to match the architecture document.

The CMake target currently uses `eldoria_network` and `Eldoria::Network` instead of the shorter net target spelling because the connector blocked that exact automated write.

This can be renamed later from a local development environment if desired.
