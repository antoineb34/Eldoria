# Binary

Lowest-level, domain-independent byte operations.

This layer owns byte reading/writing and compression primitives. It does not
know about cache storage, game assets, runtime systems, or applications.
